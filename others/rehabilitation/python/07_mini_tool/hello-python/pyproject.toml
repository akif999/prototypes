[project]
name = "hello-python"
version = "0.1.0"
description = ""
authors = [
    {name = "akif999",email = "aki01nf28@gmail.com"}
]
readme = "README.md"
requires-python = ">=3.12,<3.15"
dependencies = [
]

[tool.poetry]
name = "hello-python"
packages = [{include = "hello_python", from = "src"}]

[tool.poetry.scripts]
hello-python = "hello_python.__main__:main"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[dependency-groups]
dev = [
    "pyinstaller (>=6.18.0,<7.0.0)"
]
