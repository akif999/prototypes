Q1

次のコードの出力を答えてください：

int arr[] = {1,2,3,4};
int *p = arr;

printf("%d\n", ++*p);      // ?
printf("%d\n", *(p++));    // ?
printf("%d\n", *p);        // ?

A. 以下です。
2
2
1

Q2

次のコードは危険（バグ）です。どこが問題で、どう直すべきか説明してください。

char *s = "hello";
s[0] = 'H';

A. ??? わかりそうだけどわからない

Q3

次のコードの問題点を答えてください：

char *p = malloc(5);
strcpy(p, "ABCDE");

A. 終端文字の要素数が足りていない(もしくは終端文字を上書きしている)
